# Car Rental Admin Panel - Інструкція з налаштування

## 📋 Що було додано

✅ **MongoDB Atlas** - база даних для зберігання автомобілів  
✅ **Cloudinary** - завантаження та зберігання фото автомобілів  
✅ **API Routes** - CRUD операції для автомобілів  
✅ **Адмін-панель** - зручний інтерфейс для управління автопарком  
✅ **Аутентифікація** - захист адмін-панелі паролем

---

## 🚀 Крок 1: Налаштування MongoDB Atlas

1. Перейдіть на https://www.mongodb.com/cloud/atlas
2. Створіть безкоштовний аккаунт (Free Tier)
3. Створіть новий кластер (Cluster)
4. Натисніть **Connect** → **Connect your application**
5. Скопіюйте рядок підключення (connection string)
6. Замініть `<password>` на ваш пароль

**Приклад:**
```
mongodb+srv://username:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/car-rental?retryWrites=true&w=majority
```

---

## 🖼️ Крок 2: Налаштування Cloudinary

1. Перейдіть на https://cloudinary.com
2. Зареєструйтеся (безкоштовний аккаунт)
3. В Dashboard знайдіть:
   - **Cloud Name**
   - **API Key**
   - **API Secret**

---

## ⚙️ Крок 3: Налаштування змінних оточення

Створіть файл `.env.local` в корені проекту:

```bash
# MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/car-rental?retryWrites=true&w=majority

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Node Environment
NODE_ENV=development
```

**Замініть значення** на свої облікові дані!

---

## 👤 Крок 4: Створення першого адміністратора

Запустіть скрипт для створення облікового запису адміна:

```bash
node scripts/create-admin.js
```

Введіть:
- Ім'я користувача (username)
- Email
- Пароль (мінімум 6 символів)

---

## 🏃 Крок 5: Запуск проекту

```bash
npm run dev
```

Проект запуститься на http://localhost:3000

---

## 📍 Важливі URL

- **Головна сторінка:** http://localhost:3000
- **Вхід в адмін-панель:** http://localhost:3000/admin
- **Дашборд адміна:** http://localhost:3000/admin/dashboard

---

## 🎯 Як користуватися адмін-панеллю

### Вхід
1. Перейдіть на `/admin`
2. Введіть username та пароль
3. Натисніть "Увійти"

### Додавання автомобіля
1. На дашборді натисніть "+ Додати авто"
2. Заповніть форму:
   - **Назва:** Mercedes-Benz E 220 d 4MATIC
   - **Зображення:** завантажте фото (автоматично на Cloudinary)
   - **Теги:** Преміум, Автомат, Дизель, Седан
   - **Застава:** 2 500 $
   - **Ціноутворення:** заповніть періоди та ціни
3. Натисніть "Зберегти"

### Редагування автомобіля
1. Натисніть "Редагувати" на картці автомобіля
2. Змініть потрібні поля
3. Натисніть "Зберегти"

### Видалення автомобіля
1. Натисніть "Видалити" на картці автомобіля
2. Підтвердіть видалення

---

## 📁 Структура проекту

```
/app
  /admin                    # Адмін-панель
    /dashboard              # Дашборд для управління авто
    page.tsx                # Сторінка входу
  /api
    /auth                   # API аутентифікації
      /login
      /logout
    /cars                   # API для автомобілів (CRUD)
      /[id]                 # Окремий автомобіль
    /upload                 # API завантаження фото на Cloudinary
  page.tsx                  # Головна сторінка (оновлена для БД)

/lib
  mongodb.ts                # Підключення до MongoDB

/models
  Admin.ts                  # Модель адміністратора
  Car.ts                    # Модель автомобіля

/scripts
  create-admin.js           # Скрипт створення адміна
```

---

## 🔄 Як працює інтеграція

1. **Головна сторінка** (`page.tsx`) автоматично завантажує автомобілі з MongoDB
2. **Дані кешуються** на 60 секунд для швидкості
3. **Фото завантажуються** на Cloudinary і зберігаються як URL
4. **API автоматично** оновлює дані при додаванні/редагуванні

---

## ⚠️ Важливо для продакшену

Перед деплоєм на Vercel/Netlify:

1. Додайте змінні оточення в налаштуваннях платформи
2. Змініть `NODE_ENV=production`
3. В MongoDB Atlas додайте IP адресу `0.0.0.0/0` в Network Access
4. Використовуйте сильні паролі для адміністраторів

---

## 🐛 Можливі проблеми

### Помилка підключення до MongoDB
- Перевірте правильність MONGODB_URI
- Перевірте Network Access в MongoDB Atlas
- Переконайтесь, що пароль не містить спецсимволів (або екрануйте їх)

### Помилка завантаження фото
- Перевірте облікові дані Cloudinary
- Переконайтесь, що файл є зображенням
- Перевірте розмір файлу (макс. 10MB)

### Не працює адмін-панель
- Переконайтесь, що створили адміністратора
- Перевірте правильність username/password
- Очистіть cookies браузера

---

## 📞 Потрібна допомога?

Якщо виникли питання - звертайтесь! 🚀
